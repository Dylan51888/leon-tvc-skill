---
name: leon-tvc-skill
description: 把产品主视觉 + 分镜表变成一支可交付的 15 秒商业 TVC。适用于电商产品、礼盒、饮品、美妆等实拍类商单。当用户说"用 leon-tvc-skill 出片""按分镜表生成 TVC""做一支 15 秒广告片"时使用。
---

# 里昂 · TVC 成片指令模板

把已经确认的**产品主视觉**和**分镜表**，变成一支可以交付给客户的商业广告片。

这个 Skill 解决的不是"能不能生成视频"，而是"生成出来能不能交付"——核心是**产品一致性约束**。

## 使用前提

调用前必须已经具备两样东西，缺一不可：

1. **产品主视觉**：至少 1 张、建议 3～5 张同一产品的商业图，色调纹样统一
2. **分镜表**：含时间轴、参考图片、画面内容、景别、运镜，总时长已校验

两样都没有的话，先让用户补齐再调用本 Skill，不要自己编。

## 执行流程

### 第一步：读取并复述约束

从用户提供的素材中提取并向用户复述以下信息，等待确认：

- 产品名称、产品的不可变特征（外形比例、纹样、配色、文字/LOGO、材质）
- 分镜表的镜头数与各镜头时长，核算总时长
- 成片比例（横屏 16:9 / 4:3，竖屏 9:16）与总时长

### 第二步：生成参数确认清单

生成前必须先输出一份完整清单给用户确认，不要直接开始生成：

```
模型：seedance_2.5（固定）
任务类型：图生视频（基于 N 张参考图）
时长：__ 秒（用户指定）
比例：__（用户指定）
参考素材：
  · 产品母图：__
  · 商业图（分镜视觉基准）：__
镜头分解：
  0:00.0–0:02.5（2.5s）镜头1 …
  …
时长校验：2.5 + 3 + 3 + 3.5 + 3 = 15.0s ✓
一致性约束：（见下）
排除项：（见下）
```

用户回复确认后再生成。

### 第三步：生成

调用 Seedance 2.5，指令中必须原样带上下面两组约束。

**一致性约束（必须保持）**

- 产品比例、外形结构与母图一致
- 纹样、线条、色块的位置与母图一致
- 品牌文字、LOGO、印章的字形与位置与母图一致
- 材质质感与色温全片统一
- 多件产品的相对位置关系与母图一致

**排除项（不要出现）**

- 人物、手、肢体
- 母图中不存在的道具
- 宫殿、龙凤等泛化东方元素（除非分镜表明确要求）
- 任何知名 IP 形象
- 额外文字、字幕、水印

**收尾要求**

- 结尾保留完整产品定格，不少于 3 秒
- 保留 BGM 到片尾，不要硬切

### 第四步：交付前自检

出片后，逐项核对并把结果告诉用户：

| 校准项 | 检查什么 |
| --- | --- |
| 画面 | 产品特征有没有跑偏；有没有多出手、人物、水印 |
| 分镜 | 节奏是否顺；产品露出是否足够；结尾定格是否完整 |
| 配乐 | 是否可商用；情绪与品牌调性是否吻合 |

明确告诉用户：**这是初版，交付客户前必须人工再过一遍这三项。**

## 常见返工点

- **产品变形**：约束写得不够具体。把"保持一致"改写成可判断的条目——"礼盒长宽比 2:1""金线位于盒盖上沿 1/3 处"
- **多出手/人物**：排除项没有前置。把排除项放在指令靠前的位置，不要压在末尾
- **时长对不上**：分镜表里各镜头时长没有先求和校验。生成前先算一遍
- **风格漂移**：参考图里混进了风格不一致的图。只喂同一组商业图

## 不适用的情况

- 没有真实产品图，只有文字描述 → 先出图
- 需要真人出镜 → 本 Skill 明确排除人物
- 超过 30 秒的长片 → 拆成多段分别生成再剪
